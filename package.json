{
  "name": "jprez-whatsapp-bot",
  "version": "1.3.0",
  "description": "Bot de WhatsApp para Constructora JPREZ - Powered by Claude API",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.34.3",
    "@vercel/functions": "^3.4.3"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "vitest": "^4.1.4"
  },
  "scripts": {
    "prepare": "husky",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e-suite": "vitest run --config tests/e2e-suite/vitest.config.mjs",
    "test:e2e-suite:report": "vitest run --config tests/e2e-suite/vitest.config.mjs --reporter=verbose"
  }
}
